-- ===================================================================
-- マイグレーション詳細リスト（最新30件）
-- ===================================================================
-- どのマイグレーションが適用されているか確認
-- ===================================================================

SELECT 
    version,
    name
FROM supabase_migrations.schema_migrations
ORDER BY version DESC
LIMIT 30;
