# Supabase リモート環境確認ガイド

## 📋 概要

このガイドでは、3つのSQLファイルを使用してSupabase本番環境の完全な状態を把握します。

---

## 📁 提供されているSQLファイル

### 1. `remote_status_check.sql`
**目的**: データベースの基本的な状態確認

**確認内容**:
- ✅ 全テーブルのデータ数とサイズ
- ✅ 全ビューの存在確認
- ✅ 主要テーブルの詳細データ数
- ✅ ビューの動作確認
- ✅ 関数・トリガーの一覧
- ✅ **マイグレーション履歴（実際に適用済み）**
- ✅ テーブル構造（カラム一覧）
- ✅ インデックス一覧
- ✅ RLSポリシー一覧
- ✅ 最新データの日時
- ✅ データベースサイズ

### 2. `remote_backup_latest.sql`
**目的**: 重要なマスターデータのバックアップ

**バックアップ対象**:
- `asset_prices` - 資産価格マスター
- `daily_exchange_rates` - 為替レート（最新30日分）
- `entities` - エンティティマスター
- `profiles` - ユーザープロフィール
- `wallet_connections` - ウォレット接続情報
- `exchange_connections` - 取引所接続情報

**出力形式**: INSERT文（そのまま実行可能）

### 3. `remote_dependencies_check.sql` ⭐ NEW
**目的**: スキーマの依存関係と計算式の詳細抽出

**確認内容**:
- ✅ **ビューの完全な定義**（フォーマット済み）
- ✅ **ビュー間の依存関係**（どのビューがどのテーブル/ビューに依存しているか）
- ✅ **計算カラムとデフォルト値**（GENERATED列、DEFAULT式）
- ✅ **全制約の詳細**（PRIMARY KEY, FOREIGN KEY, UNIQUE, CHECK）
- ✅ **外部キー関係マップ**（参照関係の可視化）
- ✅ **関数の完全な定義**（ストアドプロシージャ、トリガー関数のソースコード）
- ✅ **トリガーの詳細**（トリガー定義とトリガー関数の紐付き）
- ✅ **シーケンスの現在値**（自動採番の状態）
- ✅ **インデックスの詳細**（構成カラムと種類）
- ✅ **ビューのカラム情報**（計算カラムを含む）
- ✅ **RLSポリシーの詳細**（式の読みやすい形式）
- ✅ **エクステンション一覧**

---

## 🎯 実行方法

### ステップ1: SQL Editorを開く
**本番環境**:  
https://supabase.com/dashboard/project/ymddtgbsybvxfitgupqy/sql/new

### ステップ2: 各SQLファイルを順番に実行

#### A. 基本状態確認（`remote_status_check.sql`）
1. ファイルの内容をコピー
2. SQL Editorにペースト
3. 各クエリを順番に実行（または全部まとめて実行）
4. 結果を確認・保存

**重要な確認項目**:
- マイグレーション履歴の最新バージョン
- 各テーブルのデータ数
- ビューがエラーなく動作しているか

#### B. マスターデータバックアップ（`remote_backup_latest.sql`）
1. ファイルの内容をコピー
2. SQL Editorにペースト
3. 実行
4. **結果のINSERT文をコピーして、テキストファイルとして保存**
   - 保存先例: `backups/master_data_backup_YYYYMMDD.sql`

#### C. 依存関係・計算式確認（`remote_dependencies_check.sql`）
1. ファイルの内容をコピー
2. SQL Editorにペースト
3. 各クエリを順番に実行
4. 結果を確認・保存

**特に重要な確認項目**:
- **クエリ1**: ビューの完全な定義（`all_transactions`など重要ビュー）
- **クエリ2**: ビュー間の依存関係（ビューの削除/変更時に影響範囲を把握）
- **クエリ4**: 全制約（外部キー、CHECK制約など）
- **クエリ5**: 外部キー関係（テーブル間の参照関係）
- **クエリ6**: 関数の完全な定義（トリガー関数のロジック）

---

## ✅ これで実現できること

### 1. 破損時の即座の復旧 🔧
- ✅ 最新のマスターデータバックアップを保持
- ✅ ビュー定義を完全に把握（手動で再作成可能）
- ✅ 関数・トリガーの定義を保持（再作成可能）
- ✅ 制約・インデックスの完全なリスト

### 2. 開発時の正確な状況把握 📊
- ✅ **実際に適用されているマイグレーション**を確認
- ✅ **ビューの依存関係**を理解（変更時の影響範囲）
- ✅ **計算カラムの式**を把握（ローカル開発との整合性）
- ✅ **外部キー制約**を把握（データ投入時のエラー回避）
- ✅ **RLSポリシー**を把握（権限エラーのデバッグ）

---

## 📝 結果の保存推奨

実行結果を以下のように保存することをお勧めします：

```
backups/
├── status_check_20260120.txt           # remote_status_check.sqlの実行結果
├── master_data_backup_20260120.sql     # remote_backup_latest.sqlの実行結果
└── dependencies_20260120.txt           # remote_dependencies_check.sqlの実行結果
```

---

## 🔄 定期実行の推奨

以下のタイミングで定期的に実行することをお勧めします：

- **毎週**: 基本状態確認（`remote_status_check.sql`）
- **開発前**: 依存関係確認（`remote_dependencies_check.sql`）
- **重要な変更前後**: マスターデータバックアップ（`remote_backup_latest.sql`）

---

## ⚠️ 注意事項

- **read-onlyクエリ**: すべてSELECT文なので、データを変更することはありません
- **実行時間**: データ量により数秒〜数十秒かかる場合があります
- **大きな結果**: ビュー定義や関数定義は非常に長くなる場合があります

---

## 🆘 トラブルシューティング

### エラー: "permission denied"
→ SQL Editorで実行する際、supabase_admin権限で実行されているか確認

### エラー: "relation does not exist"
→ テーブル名やビュー名が変更されている可能性。クエリ1（テーブル一覧）で確認

### 結果が表示されない
→ データが存在しない可能性。WHERE句の条件を確認

---

*このガイドを使用して、Supabaseリモート環境の完全な状態把握と、安全な開発・復旧体制を構築してください。*
